---
title: Datasets
description: A dataset is a collection of datapoints to be used for fine-tuning.
icon: database
---

## Creating a Dataset

### Via the SDK

To create a dataset, use `montelo.datasets.create`;

<CodeGroup>

```typescript Node
const dataset = await montelo.datasets.create({
  name: "Article Dataset",
  description: "Datasets for AI articles"
});
```

```python Python
dataset = montelo.datasets.create(
  name="Article Dataset",
  description="Datasets for AI articles",
)
```
</CodeGroup>


### Via the UI

You can create a dataset via the UI by navigating to the Datasets page and clicking on the `Create Dataset` button.

<Frame type="glass">
  <img src="/assets/datasets/create.png"/>
</Frame>
